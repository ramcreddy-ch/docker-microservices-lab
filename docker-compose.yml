version: '3.8'
services:
  api:
    build: ./api
    ports:
      - "5000:5000"
    depends_on:
      - redis
  worker:
    build: ./worker
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
